name: OpenSpec Validate

on:
  pull_request:
    paths:
      - "openspec/**"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: OpenSpec lint
        run: ./build/openspec-lint.sh
