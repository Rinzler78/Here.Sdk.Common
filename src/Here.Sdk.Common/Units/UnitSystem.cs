namespace Here.Sdk.Common.Units;

/// <summary>Measurement unit system preference.</summary>
public enum UnitSystem
{
    /// <summary>Metric system (SI).</summary>
    Metric = 0,

    /// <summary>Imperial system (miles, yards, feet).</summary>
    Imperial = 1,
}
